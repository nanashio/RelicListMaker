import {
    setHidden,
    clearChildren,
    updateStatusElement,
    applyInlineStyles,
    ensureElement
} from './utils/dom.js';

window.galleryDomUtils = {
    setHidden,
    clearChildren,
    updateStatusElement,
    applyInlineStyles,
    ensureElement
};
